# Phase 1 backtest harness — Postgres credentials (read-only).
# Copy this file to .env and fill in your connection string.
# Do NOT commit .env — it is git-ignored.

HARNESS_DATABASE_URL=postgresql://harness_readonly.<project>:<password>@<host>:5432/postgres?sslmode=require
