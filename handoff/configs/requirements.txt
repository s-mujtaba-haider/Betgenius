# Python dependencies for the MLB Phase 1 validation pipeline
# (betgenius/harness/uplift/*.py). Python 3.14 is what it was built and verified
# on; 3.11+ should work, but pin pandas >= 3 because the date handling in
# frames.to_day depends on pandas 3 semantics.
#
#   pip install -r requirements.txt
#
# The exact versions the shipped results were produced with are in the comments.

pandas>=3.0            # 3.0.3
numpy>=2.4             # 2.4.6
scikit-learn>=1.9      # 1.9.0
scipy>=1.18            # 1.18.0
statsmodels>=0.14      # 0.14.6
openpyxl>=3.1          # 3.1.5   (only needed by build_workbook.py)

# Nothing else is imported by the pipeline. In particular it does NOT need
# lightgbm, xgboost or catboost: the boosted member is scikit-learn's
# HistGradientBoostingClassifier.
